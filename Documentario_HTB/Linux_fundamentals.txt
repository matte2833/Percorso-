HTB - Linux_Fundamentals (36%)- 31/10/2025

#obbiettivo:
imparare e diventare più famigliare con  i comandi di linux attraverso esperienza tecnica.

#ambiente:

- Kali VM.
- terminale.
- 4h.

#comandi principali:

`ls`, `cat`, `cd`, `mkdir`, `rmdir`, `rm -r`, `grep`, `find`, `su`, `du`, `apt`, `dpkg`, `sudo`, `ssh`, `whoami`, `pwd`, `echo`, `touch`, `cp`, `mv`, `mkdir -p`, `apropos`, `man`, `nano`, `uname`, `hostname`, `id`, `cd ..`

#concetti appresi:

- File system navigation:`ls`, `cd`, `pwd` per muoversi e localizzarsi.  
- Lettura file: `cat`, `less`, `head`, `tail` per leggere contenuti.  
- Creazione/modifica file: `touch`, `nano`, `mkdir`, `rm`, `mv`, `cp`.  
- Permissions & identity: `id`, `whoami`, `su`, `sudo`.  
- Package management:`apt`, `dpkg` (concetti base).  
- `>` per redirect output su file (stdout → file).  
-  Ricerca:`grep`, `find` per cercare contenuti e file.
-  byte: `du` per vedere dimensioni file/cartelle.

pensiero: 

corso abbastanza tosto devo dire.
ricopre una gamma IMMENSA di comandi partendo da ls,cd e cat che sono comandi basici andando a comandi come apt e dpgk che servono per installare le applicazionni.
